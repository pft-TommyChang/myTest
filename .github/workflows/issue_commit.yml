name: On Push Handling - 3

on:
  push:
    branches:
      - '**'

permissions:
  contents: write
  issues: write

jobs:
  call-shared-workflow:
    uses: pft-TommyChang/Sirius-Release/.github/workflows/issue_commit_reuseable.yml@main
    secrets: inherit
