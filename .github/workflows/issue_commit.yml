name: On Push Handling - 2

on:
  push:
    branches:
      - '**'

permissions:
  contents: write
  issues: write

jobs:
  call-shared-workflow:
    uses: pft-TommyChang/Sirius-Release/.github/workflows/issue_commit_reuseable@main
    secrets: inherit
